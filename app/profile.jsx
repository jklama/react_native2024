import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Profile = () => {
  return (
    <View>
      <Text className="text-bold font-plight">profile</Text>
    </View>
  );
};

export default Profile;

const styles = StyleSheet.create({});
